<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.panda.mapper.PackageOrderMapper">

    <insert id="insert" parameterType="com.panda.entity.PackageOrder" useGeneratedKeys="true" keyProperty="id">
        insert into package_order (user_id, package_id, price, order_status, pay_status, create_time)
        values (#{userId}, #{packageId}, #{price}, #{orderStatus}, #{payStatus}, #{createTime})
    </insert>

</mapper>
